name: Checkout Demo1

on:
  push:
    branches:
      - main

jobs:
  updated-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List repository files
        run: |
          ls -l
